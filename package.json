{
  "name": "protontweaks-db",
  "type": "module",
  "scripts": {
    "ci": "bun install --frozen-lockfile",
    "build:api": "bun api/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "handlebars": "^4.7.8"
  }
}
